// README at: https://github.com/devcontainers/templates/tree/main/src/python
{
    "name": "Python 3",
    "build": {
        "dockerfile": "Dockerfile",
        "args": {
            // Options arm64 or amd64
            "PLATFORM": "arm64"
        }
    },
    "postCreateCommand": "uv add polars ipykernel pip ruff pandas",
    "remoteUser": "root",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "charliermarsh.ruff"
            ]
        },
        "settings": {
            "python.formatting.provider": "charliermarsh.ruff",
        }
    }
}
